<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OptionMenu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OptionMenu" parent="Base.Theme.OptionMenu" />
    <!-- Base application theme... -->

    <!-- 1. Style to make the three dots (overflow icon) BLACK -->
    <style name="ToolbarThemeBlack" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorSecondary">#000000</item>
    </style>

    <!-- 2. Style to make the Popup Menu background dark and text WHITE -->
    <!-- This makes the dropdown box WHITE and the text BLACK strictly -->
    <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style>


<!-- This makes the dropdown box black and the text white -->
    <!-- <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style> -->


<!-- This makes the dropdown box black and the text white -->
    <!-- <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style> -->


<!-- This makes the dropdown box black and the text white -->
    <!-- <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style> -->

    <!-- This makes the dropdown box WHITE and the text BLACK strictly -->
    <!-- <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style> -->

    <!-- This makes the dropdown box WHITE and the text BLACK strictly -->
    <!-- <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style> -->

    <!-- This makes the dropdown box WHITE and the text BLACK strictly -->
    <!-- <style name="MenuPopupWhiteText" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style> -->
</resources>